import React from 'react';
import PropTypes from 'prop-types';
import classnames from 'classnames';
import styles from './Column.module.scss';


const Button = ({ name, items }) => (
  <div>
  </div>
);

export default Button;
